# Comma-separated list of domains agent-browser is allowed to navigate to.
# Leave blank during initial testing, but always set this before running
# unattended or production automation.
AGENT_BROWSER_ALLOWED_DOMAINS=

# Optional. Only needed if you use natural-language mode:
#   agent-browser chat "go to example.com and get the page title"
AI_GATEWAY_API_KEY=
